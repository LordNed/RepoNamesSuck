<UserControl x:Class="WEditor.UI.View.SceneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:self="clr-namespace:WindEditor.UI"
             xmlns:windeditor="clr-namespace:WEditor.WindWaker;assembly=EditorCore"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <HierarchicalDataTemplate x:Key="ChildTemplate">
            <TextBlock Text="{Binding FileName}"/>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate x:Key="ArchiveViewTemplate" ItemsSource="{Binding Files}" ItemTemplate="{StaticResource ChildTemplate}">
            <TextBlock Text="{Binding Name}"/>
        </HierarchicalDataTemplate>
    </UserControl.Resources>
    <Grid>
        <GroupBox Header="Scene View" Margin="2,0">
            <TreeView ItemsSource="{Binding ArchiveList}" ItemTemplate="{StaticResource ArchiveViewTemplate}"/>
        </GroupBox>
    </Grid>
</UserControl>
