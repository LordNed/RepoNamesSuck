<UserControl x:Class="WEditor.UI.View.Inspector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBlock Text="Test" VerticalAlignment="Top"/>
        <ItemsControl Margin="0,16,0,0" ItemTemplateSelector="{StaticResource InspectorItemTemplateSelector}" DataContext="{Binding SelectedEntity}" ItemsSource="{Binding Properties}"/>

    </Grid>
</UserControl>
