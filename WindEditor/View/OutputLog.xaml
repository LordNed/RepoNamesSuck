<UserControl x:Class="WEditor.UI.View.OutputLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="268" Width="736">
    <Grid>
        <Grid.Background>
            <ImageBrush/>
        </Grid.Background>
        <RichTextBox>
            <FlowDocument>
                <Paragraph>
                    <Run Text="{Binding Messages, Mode=OneWay}"/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>

    </Grid>
</UserControl>
